// API configuration for local and container deployment.
// Empty by default so the production frontend can use the nginx /v1 proxy.
export const API_URL = '';

// Re-export mapping mode constants for backward compatibility
export { MappingMode } from '../constants/mapping-modes';
